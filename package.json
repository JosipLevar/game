{
  "name": "gridfix",
  "version": "1.0.0",
  "private": true,
  "description": "GridFix: Distribution Roguelike — static, framework-free NN grid dispatcher puzzle game.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  }
}
